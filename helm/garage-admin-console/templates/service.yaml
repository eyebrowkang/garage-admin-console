apiVersion: v1
kind: Service
metadata:
  name: {{ include "garage-admin-console.fullname" . }}
  labels:
    {{- include "garage-admin-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "garage-admin-console.selectorLabels" . | nindent 4 }}
