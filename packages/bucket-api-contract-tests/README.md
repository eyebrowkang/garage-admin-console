# @garage/bucket-api-contract-tests

Conformance test suite for the Bucket Backend API (see `designs/mf-integration-plan.md` §2.4).

**Status:** placeholder. Scaffolding only.

When implemented, CI runs this suite against both `@garage-admin/api` and `@s3-browser/api` to prove they expose the identical contract.
